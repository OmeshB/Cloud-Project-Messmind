# ── Staging environment overrides ───────────────────────
replicaCount:
  frontend: 1
  backend: 1

image:
  frontend:
    tag: staging   # Pinned to the staging Docker tag
  backend:
    tag: staging

service:
  frontend:
    type: LoadBalancer
  backend:
    type: ClusterIP

name: CI/CD Pipeline

on:
  push:
    branches:
      - main   # triggers when you push code

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build Step
        run: echo "Building app..."

      - name: Deploy Step
        run: echo "Deploying to Azure..."
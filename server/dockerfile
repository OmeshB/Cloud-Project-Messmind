FROM node:20

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
# Build the server bundle using vite
RUN npm run build:server

EXPOSE 5000

# Run the compiled Express server (npm start runs dist/server/node-build.mjs)
CMD ["npm", "start"]